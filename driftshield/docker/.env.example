# DriftShield dogfood/prod docker env example
ENVIRONMENT=production

# Required
API_KEY=replace-with-a-long-random-api-key

# Database
DB_USER=drift
DB_PASSWORD=replace-with-a-strong-db-password
DB_NAME=driftshield

# Application
PORT=8080
LOG_LEVEL=info
WORKERS=1
MAX_REQUEST_BYTES=26214400
